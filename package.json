{
  "name": "customers-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start:server": "node server.js",
    "start:server:watch": "nodemon -e yaml,js,json server.js",
    "setup-db": "docker-compose up -d db",
    "migrate-db": "db-migrate up",
    "start": "npm-run-all migrate-db start:server",
    "start:watch": "npm-run-all migrate start:server:watch",
    "test": "jest --testRunner='jest-circus/runner' --maxWorkers=1",
    "test:setup-db": "docker-compose up -d test_db",
    "test:migrate-db": "db-migrate up -e test",
    "test:setup-and-run": "npm run test:setup-db && sleep 10 && npm run test:migrate-db && npm test",
    "lint": "semistandard --fix"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint"
    }
  },
  "jest": {
    "testRunner": "jest-circus/runner",
    "testEnvironment": "node",
    "setupFilesAfterEnv": [
      "<rootDir>/tests/setupTests.js"
    ],
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  },
  "semistandard": {
    "env": [
      "node",
      "jest"
    ],
    "ignore": [
      "migrations/**"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "npm-run-all": "^4.1.5",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "jest": "^26.0.1",
    "jest-circus": "^26.0.1",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2",
    "swagger-jsdoc": "^4.0.0",
    "swagger-ui-express": "^4.1.4"
  }
}
